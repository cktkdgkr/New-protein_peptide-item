# 프로젝트 메모리 (Project Memory)

본 파일은 이 repository에서 Claude Code 세션을 시작할 때 자동으로 로드되는 프로젝트 지침입니다.

---

## 신규 아이템 Deep Dive 조사 오케스트레이터 규칙

이 repo는 신규 아이템(제품·기술·서비스)에 대한 다중 서브 에이전트 기반 Deep Dive 조사 보고서를 산출하는 워크플로우를 운영합니다. 기본 워크플로우(8개 리서치 에이전트 [A]~[H] + 검증 에이전트 → 최종 Word 보고서)는 사용자의 오케스트레이터 프롬프트에 정의되어 있으며, 본 메모리는 **추가/오버라이드되는 규칙**만 보관합니다.

### 적용 규칙

#### R1. 최종 보고서에 그림(figure) 적극 활용
- **원칙**: 최종 보고서(.docx) 작성 시, **"그림 설명이 유리하다고 판단되는 지점"에 한하여 그림을 적극적으로 활용 혹은 생성하여 입력하여 설명**한다.
- **생성 방법**: `matplotlib` 및/또는 `Pillow`로 PNG 이미지를 생성하여 `python-docx`의 `add_picture()`로 .docx에 직접 삽입한다.
- **그림이 유리한 대표 지점 (판단 기준)**:
  - 시장 규모·CAGR 시계열 → **막대/선 그래프**
  - 경쟁사 포지셔닝 (가격×성능, 매력도×난이도) → **2×2 매트릭스, 산점도**
  - SWOT, 진입장벽 점수표 → **레이더 차트, 히트맵**
  - 밸류체인·프로세스 흐름 → **블록 다이어그램** (Pillow로 박스+화살표)
  - 특허 출원 추이, 점유율 → **막대 그래프, 도넛 차트**
  - White Space 분석 → **2×2 quadrant 차트**
  - 기술 비교 (다축 성능) → **레이더 차트**
- **금지/주의**:
  - 단순 텍스트 나열로 충분한 경우(예: 단일 수치 1개)에는 그림을 만들지 않는다 — 보고서가 과하게 무거워지는 것을 방지.
  - 그림 안의 모든 수치·라벨은 본문 표/출처와 일치해야 한다(임의 수치 금지).
  - 그림 캡션에는 "(출처: …, 발행일 …)" 형식으로 데이터 소스 명시.
  - 한글 라벨이 필요한 경우 `matplotlib`의 한글 폰트(`NanumGothic`, `Malgun Gothic` 등) 설정 필수.
- **빌드 스크립트**: 그림 생성 로직은 `build_report.py`에 함수로 분리하여 재현 가능하게 유지한다.

---

## 환경 정보

- 작업 디렉토리: `/home/user/New-protein_peptide-item/`
- 중간 산출물 폴더: `./research/` (.md 파일 8종 + VERIFICATION.md + 보조 자료)
- 최종 산출물: `./research/FINAL_REPORT.docx`
- 사용 가능 라이브러리: `python-docx`, `matplotlib`, `Pillow`, `lxml` (이미 설치됨)
- 기본 브랜치: `claude/orchestrator-item-research-77CmR`
